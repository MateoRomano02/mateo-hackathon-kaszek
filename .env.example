# AI Provider
# Set to "mock" for demo mode (default), "anthropic" to use real API via backend proxy
VITE_AI_PROVIDER=mock

# Backend proxy URL (only needed when VITE_AI_PROVIDER=anthropic)
# NEVER put the real Anthropic API key here — use a backend proxy
VITE_API_BASE_URL=http://localhost:3001
